- currentRound = (currentRound)?currentRound:{number:'-',is_open:false}
div.rounds Current Round: #{currentRound.number} &nbsp
  if (currentRound.is_open) 
    span.open-round (open)
  else 
    span.closed-round (closed)